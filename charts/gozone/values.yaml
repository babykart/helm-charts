# Default values for gozone.

deployment:
  # -- Deployment annotations
  annotations: {}
  # -- Deployment labels
  labels: {}
  # -- Deployment replica count
  replicas: 1
  # -- Deployment revision history limit count
  revisionHistoryLimit: 1
  strategy:
    # -- Deployment strategy type
    type: RollingUpdate

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  # -- This sets the repistry
  registry: ghcr.io
  # -- This sets the repository
  repository: babykart/gozone
  # -- This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # -- Overrides the image tag whose default is the chart appVersion.
  tag: ""

# -- This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []

# -- This is to override the chart name.
nameOverride: ""
# -- This is to override the full chart name.
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # -- Specifies whether a service account should be created.
  create: false
  # -- Automatically mount a ServiceAccount's API credentials?
  automount: false
  # -- Annotations to add to the service account.
  annotations: {}
  # -- The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template.
  name: ""

config:
  # -- GoZone secret (if you want to use an existing secret).
  # This secret must contains a key called 'config.yaml'.
  existingSecret: ""
  # -- The config.yaml data content.
  gozoneConf: ""
  #gozoneConf: |
  #  server:
  #    host: "0.0.0.0"
  #    port: 8080
  #    secret_key: "change-me-to-a-random-secret-key"
  #
  #  database:
  #    driver: "mysql"
  #    dsn: "user:password@tcp(mysql.dns.svc.cluster.local:3306)/gozone"
  #
  #  powerdns:
  #    api_url: "http://localhost:8081"
  #    api_key: "changeme"
  #    server_id: "localhost"
  #
  #  auth:
  #    session_duration_hours: 24
  #    bcrypt_cost: 12
  #
  #  logging:
  #    level: "info"
  #
  #  admin:
  #    username: "admin"
  #    password: "admin"
  #    email: "admin@gozone.local"
  #    first_name: "Admin"
  #    last_name: "User"

# -- DNS config
dnsConfig: {}
# dnsConfig:
#   options:
#   - name: ndots
#     value: "1"
#   - name: edns0

# -- DNS policy
dnsPolicy: ClusterFirst

# -- This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# -- This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

# -- Pod SecurityContext
podSecurityContext:
  fsGroup: 65532

# -- SecurityContext
securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # -- This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # -- This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 8080
  # -- Service ipFamilies
  ipFamilies:
  - IPv4
  # -- Service ipFamilyPolicy SingleStack|PreferDualStack|RequireDualStack
  ipFamilyPolicy: SingleStack

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  # -- Ingress enabled
  enabled: false
  # -- Ingress className
  className: ""
  # -- Ingress annotations
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
    # - secretName: chart-example-tls
    #   hosts:
    #     - chart-example.local

# -- Expose the service via gateway-api HTTPRoute
# Requires Gateway API resources and suitable controller installed within the cluster
# (see: https://gateway-api.sigs.k8s.io/guides/)
httpRoute:
  # -- HTTPRoute enabled.
  enabled: false
  # -- HTTPRoute annotations.
  annotations: {}
  # Which Gateways this Route is attached to.
  parentRefs:
  - name: gateway
    sectionName: http
    # namespace: default
  # Hostnames matching HTTP header.
  hostnames:
  - chart-example.local
  # List of rules and filters applied.
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /headers
  #   filters:
  #   - type: RequestHeaderModifier
  #     requestHeaderModifier:
  #       set:
  #       - name: My-Overwrite-Header
  #         value: this-is-the-only-value
  #       remove:
  #       - User-Agent
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /echo
  #     headers:
  #     - name: version
  #       value: v2

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# -- This is to setup the liveness probe.
# More information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /health/live
    port: http
  initialDelaySeconds: 3
  periodSeconds: 30

# -- This is to setup the readiness probe.
readinessProbe:
  httpGet:
    path: /health/ready
    port: http
  initialDelaySeconds: 3
  periodSeconds: 30

# -- Additional environment variables.
env: []

# -- Additional volumes on the output Deployment definition.
volumes: []
  # - name: foo
  #   secret:
  #     secretName: mysecret
  #     optional: false

# -- Additional volumeMounts on the output Deployment definition.
volumeMounts: []
  # - name: foo
  #   mountPath: "/etc/foo"
  #   readOnly: true

# -- This sets the nodeSelector labels
nodeSelector: {}

# -- Tolerations
tolerations: []

# -- Affinity
affinity: {}
